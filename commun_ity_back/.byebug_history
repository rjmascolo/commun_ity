exit
JWT.decode(token, 'secret', false)[0]['id']
JWT.decode(token, 'secret', false)[0]['id]
JWT.decode(token, 'secret', false)[0]
JWT.decode(token, 'secret', false)
JWT.decode(token)
token
self.memberships.select { |membership| membership['member_type'] == 'manager'}
